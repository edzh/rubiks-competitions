import React, { Component } from 'react';
import Create from './components/Create';

class Main extends Component {
  render() {
    return(
      <div>
        <Create />
      </div>
    );
  }
}

export default Main;
